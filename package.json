{
  "name": "notizen-api",
  "version": "1.0.0",
  "description": "Notizen-API mit Express.js Backend und Vite Frontend",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\"",
    "start": "npm run start -w backend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "seed": "npm run seed -w backend",
    "test": "npm run test -w backend",
    "test:coverage": "npm run test:coverage -w backend",
    "test:watch": "npm run test:watch -w backend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "lint:fix": "npm run lint:fix -w backend && npm run lint:fix -w frontend",
    "format": "npm run format -w backend && npm run format -w frontend",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "notes",
    "api",
    "express",
    "mongodb",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
